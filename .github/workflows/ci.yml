name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install runtime and test dependencies
        run: python -m pip install -r requirements.txt -r requirements-dev.txt

      - name: Lint (ruff)
        # Lint the whole project (ruff.toml excludes the vendored deps/ tree),
        # so every extracted module is covered, not just main.py + core.
        run: python -m ruff check .

      - name: Byte-compile main.py (syntax check)
        # Cheap guard for syntax errors in the application entrypoint.
        run: python -m py_compile main.py

      - name: Run tests
        run: python -m pytest
