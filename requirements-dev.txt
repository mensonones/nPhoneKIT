# Development / test dependencies for nPhoneKIT.
#
# Install with:
#     python3 -m pip install -r requirements-dev.txt
#
# These are NOT needed to run the app, only to run the test-suite and linters.

pytest
ruff>=0.5,<1.0
