[pytest]
# Only collect nPhoneKIT's own tests; never descend into the vendored
# deps/mtkclient tree (it ships its own unrelated test files that require
# extra dependencies and are not part of this project's suite).
testpaths = tests
norecursedirs = deps .git __pycache__
addopts = -q
